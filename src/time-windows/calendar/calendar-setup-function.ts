// ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".
import * as path from 'path';
import * as lambda from 'aws-cdk-lib/aws-lambda';
import { Construct } from 'constructs';

/**
 * Props for CalendarSetupFunction
 */
export interface CalendarSetupFunctionProps extends lambda.FunctionOptions {
}

/**
 * An AWS Lambda function which executes src/time-windows/calendar/calendar-setup.
 */
export class CalendarSetupFunction extends lambda.Function {
  constructor(scope: Construct, id: string, props?: CalendarSetupFunctionProps) {
    super(scope, id, {
      description: 'src/time-windows/calendar/calendar-setup.lambda.ts',
      ...props,
      runtime: new lambda.Runtime('nodejs14.x', lambda.RuntimeFamily.NODEJS),
      handler: 'index.handler',
      code: lambda.Code.fromAsset(path.join(__dirname, '../../../assets/time-windows/calendar/calendar-setup.lambda')),
    });
    this.addEnvironment('AWS_NODEJS_CONNECTION_REUSE_ENABLED', '1', { removeInEdge: true });
  }
}