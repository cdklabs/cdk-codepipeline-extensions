// ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".
import * as path from 'path';
import * as lambda from 'aws-cdk-lib/aws-lambda';
import { Construct } from 'constructs';

/**
 * Props for AutorollbackFunction
 */
export interface AutorollbackFunctionProps extends lambda.FunctionOptions {
}

/**
 * An AWS Lambda function which executes src/autorollback/autorollback.
 */
export class AutorollbackFunction extends lambda.Function {
  constructor(scope: Construct, id: string, props?: AutorollbackFunctionProps) {
    super(scope, id, {
      description: 'src/autorollback/autorollback.lambda.ts',
      ...props,
      runtime: new lambda.Runtime('nodejs14.x', lambda.RuntimeFamily.NODEJS),
      handler: 'index.handler',
      code: lambda.Code.fromAsset(path.join(__dirname, '../../assets/autorollback/autorollback.lambda')),
    });
    this.addEnvironment('AWS_NODEJS_CONNECTION_REUSE_ENABLED', '1', { removeInEdge: true });
  }
}