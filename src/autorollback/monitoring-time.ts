import { Duration } from 'aws-cdk-lib';

export class MonitoringTime {
  public static conditional() {

  }

  public static duration(_time: Duration) {
  }

  constructor(public readonly value: string) {}
}